#
#                                            prg/debug/Makefile.in
#
Local_Depend =


#CLIP	= clip
#CLIP	= $(CLIPROOT)/bin/clip
#CLIPFLAGS = -O -l -w
#CLIPLIBS  =
OBJS  = init.o cld_obj.o menu.o command.o source.o values.o viewset.o viewali.o monitor.o
POBJS  = init.o cld_obj.po menu.po command.po source.po values.po viewset.po viewali.po monitor.po
.SUFFIXES: .prg .o .po

all : Makefile clip_cld$(EXESUFF)

cfg :

cleanc :
	rm -rf$(V) *.c *.b clip_cld$(EXESUFF) test$(EXESUFF)

clip_cld$(EXESUFF) : clip_cld.prg $(OBJS) Makefile $(CLIP) $(LIBCLIP)
	$(Trace_clip)
	$(CLIP) $(CLIPFLAGS) -o$@ -s -e -M $< $(OBJS)
#	$(CLIPLIBS)

install : all
	cp --remove-destination -fpu$(V) clip_cld$(EXESUFF) $(Clip_B_Dir)/

test$(EXESUFF) : test.prg Makefile
	$(Trace_clip)
	$(CLIP) $(CLIPFLAGS) -s -e -M  $<

include $(Makefile_o_in)

include $(Makefile_end_in)

