#
#                                            prg/cobra_serv/Makefile.in
#
Local_Depend =
#$(Clip_L_Dir)/libclip-codb.so
Local_Depend +=
#$(Clip_L_Dir)/libclip-r2d2.so

#CLIP	= clip
#CLIP	= $(CLIPROOT)/bin/clip
CLIPFLAGS += -n
CLIPLIBS  = -lclip-codb -lclip-r2d2
OBJS  =
#cobra_conn.o
CHS  =
#corba_serv.ch
MODULES = module1.po codbtview.po codbdlist.po
.SUFFIXES: .prg .o .po

DIRS_TO_SCAN = auth mod-ini tcp-wrap

all : Makefile $(DIRS_TO_SCAN) cobra_serv$(EXESUFF) $(MODULES)

cfg :

cleanc : $(DIRS_TO_SCAN)
	$(E_rm)rm -f$(V) *.c *.b
	$(E_rm)rm -f$(V) cobra_serv$(EXESUFF) *.pro

distcleanc :
	$(E_rm)rm -rf$(V) *.g

cobra_serv$(EXESUFF) :cobra_serv.prg $(CHS) $(OBJS) Makefile $(CLIP) $(LIBCLIP)
#	$(Trace_clip)
#	$(CLIP) $(CLIPFLAGS) -es -o$@ cobra_serv.prg $(OBJS) $(CLIPLIBS)
	$(E_clip)if [ -f $(subst .prg,.g,$<) ] ; then 							\
		$(Trace_clip_d) 	;														\
		$(CLIP) $(CLIPFLAGS) -g -o $@ -es $< $(OBJS)  $(CLIPLIBS);	\
	else 																				\
		$(Trace_clip) 	;															\
		$(CLIP) $(CLIPFLAGS) -o $@ -es $< $(OBJS) $(CLIPLIBS) ;		\
	fi

install: all
	$(E_mkD)mkdir -p$(V) $(DESTDIR)/cobra
	$(E_cp)cp --remove-destination -fpu$(V) cobra_serv$(EXESUFF) $(Clip_B_Dir)/
	$(E_cp)cp --remove-destination -fpu$(V) cobra_serv$(EXESUFF) $(DESTDIR)/cobra/
	$(E_cp)cp --remove-destination -fpu$(V) cobra_serv.ini $(DESTDIR)/cobra
	$(E_mkD)mkdir -p$(V) $(DESTDIR)/cobra/mod-ini
	$(E_cp)cp --remove-destination -fpu$(V) ./mod-ini/Makefile ./mod-ini/*.po ./mod-ini/*.prg $(DESTDIR)/cobra/mod-ini
	$(E_mkD)mkdir -p$(V) $(DESTDIR)/cobra/tcp-wrap
	$(E_cp)cp --remove-destination -fpu$(V) ./tcp-wrap/Makefile ./tcp-wrap/tcp-* $(DESTDIR)/cobra/tcp-wrap
	$(E_cp)cp --remove-destination -fpu$(V) $(MODULES) $(DESTDIR)/cobra/
	$(E_mkD)mkdir -p$(V) $(DESTDIR)/cobra/files
	$(E_cp)cp --remove-destination -fpu$(V) ./auth/pam-err-msg $(DESTDIR)/cobra/files

include $(Makefile_o_in)

include $(Makefile_end_in)

