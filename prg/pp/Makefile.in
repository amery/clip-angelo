#
#                                            prg/pp/Makefile.in
#
Local_Depend =


##CLIP	= $(CLIPROOT)/bin/clip
#CLIPFLAGS = -O
LIBS  =
OBJS =
BIN = pp$(EXESUFF)

$(BIN):	pp.prg Makefile $(CLIP) $(LIBCLIP)
#	$(Trace_clip)
#	$(CLIP) $(CLIPFLAGS) -o$@ -e -s $< $(CLIPLIBS)
	$(E_clip)if [ -f $(subst .prg,.g,$<) ] ; then 						\
		$(Trace_clip_d) 	;													\
		$(CLIP) $(CLIPFLAGS) -g -o $@ -es $< $(OBJS) $(LIBS) ;	\
	else 																			\
		$(Trace_clip) ;														\
		$(CLIP) $(CLIPFLAGS) -o $@ -es $< $(OBJS) $(LIBS) ;		\
	fi

cfg :

 all : Makefile  pp$(EXESUFF)

install : all
	$(E_cp)cp --remove-destination -fpu$(V) $(BIN) $(DESTDIR)/bin/pp_ron$(EXESUFF)
	$(E_cp)cp --remove-destination -fpu$(V) *.ch $(DESTDIR)/include

cleanc:
	$(E_rm)rm -f$(V) *.c *.ntx $(BIN) *.pp?

distcleanc :
	$(E_rm)rm -rf$(V) *.g

include $(Makefile_o_in)

include $(Makefile_end_in)

