#
#                                            prg/udb/reports/Makefile.in
#

Local_Depend =
# $(Clip_L_Dir)/libclip-gtk.so

#CLIPFLAGS = -p -O -b -w -I ./include
#CLIPLIBS  =
#CLIPFLAGS += -p

.SUFFIXES: .prg .o .po

MODULES = 	$(sort default.po )

install all : Makefile $(DIRS_TO_SCAN) $(MODULES)

cfg :

commit:
	../_cvs commit
update:
	../_cvs update -dP

cleanc : $(DIRS_TO_SCAN)
	rm -rf$(V) *.c *.b $(MODULES)
	rm -rf$(V) *.pro $(PROGS) test test2

distcleanc :
	rm -rf$(V) *.g

include $(Makefile_o_in)

include $(Makefile_end_in)

