#
#                                            prg/kamache/mod-ini/Makefile.in
#
Local_Depend =

# CLIP	= clip$(EXESUFF)
#CLIPFLAGS += -p
MODULES = kmch_hello.po mk_tmpdir.po #kmch_codb.po
.SUFFIXES: .prg .o .po

all : Makefile $(MODULES)

cfg :

cleanc :
	$(E_rm)rm -rf$(V) *.c *.b $(MODULES)

distcleanc :
	$(E_rm)rm -rf$(V) *.g

install : all
	$(E_mkD)mkdir -p$(V) $(DESTDIR)/kamache/mod-ini
	$(E_cp)cp --remove-destination -fpu$(V) Makefile *.po *.prg $(DESTDIR)/kamache/mod-ini

include $(Makefile_o_in)

include $(Makefile_end_in)

