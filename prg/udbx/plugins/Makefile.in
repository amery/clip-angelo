#
#                                            prg/udbx/plugins/Makefile.in
#

Local_Depend = $(Clip_L_Dir)/libclip-gtk.so

#CLIP	= $(CLIPROOT)/bin/clip
#CLIPFLAGS = -O -b -w -I ./include
#CLIPLIBS  =
#LIBS = ./libclip-codb$(DLLSUFF)
#CLIPFLAGS += -p
.SUFFIXES: .prg .o .po

MODULES = 	$(sort attr.po class.po counter.po default.po deposit.po \
				group.po index.po plugins.po report.po tcolumn.po tview.po user.po )

all : Makefile $(DIRS_TO_SCAN) $(MODULES)

cfg :

commit:
	../_cvs commit
update:
	../_cvs update -dP

cleanc : $(DIRS_TO_SCAN)
	rm -rf$(V) *.c *.b $(MODULES)
	rm -rf$(V) *.pro test test2

install : all

include $(Makefile_o_in)

include $(Makefile_end_in)

