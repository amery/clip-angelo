#
#                                            clip-ctcom/Makefile.in
#

Local_Depend = include/*.ch
#CLIPINCLUDE=-I$(CLIPROOT)/include
PROG = clip-ctcom
#TARGET=libclip-ctcom$(DLLSUFF)
#RTARGET=libclip-ctcom$(DLLREALSUFF)
OBJS=ct_com.o serial.o
DOC = *.html

all: Makefile $(STARGET)


cfg :
	cp --remove-destination -fpu$(V) include/*.ch $(Clip_I_Dir)/

#$(TARGET): $(OBJS)
#	$(CLIP_MAKESLIB) $(TARGET) $(OBJS)

include $(Makefile_o_in)

#hashcode.h:
#	echo '#define HASH_ferror ' $$(echo ferror| $(CLIPROOT)/bin/cliphash | cut -d' ' -f2) >hashcode.h

install:all install-lib install-doc
#	cp -fu$(V) $(TARGET) $(DESTDIR)/lib
#	mkdir -p$(V) $(DESTDIR)doc/clip-ctcom
#	cp -fu$(V) *.html $(DESTDIR)/doc/clip-ctcom

cleanc: example
	rm -rf$(V) hashcode.h $(TARGET).*

include $(Makefile_end_in)

