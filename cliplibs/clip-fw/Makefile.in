#
#                                            clip-fw/Makefile.in
#
Local_Depend = include/*.ch include/*.h

#CLIP	= $(CLIPROOT)/bin/clip
CLIPFLAGS += $(INCLUDEDIRS)
#CC = gcc
#CFLAGS = -fPIC -Wall -g -I. -I$(CLIPROOT)/include
PROG = clip-fw
CLIPLIBS  =
#FWLIB = libclip-fw.a
#FWINLIB = libclip-fw$(DLLSUFF)
#FWINLIBREAL = libclip-fw$(DLLREALSUFF)

COBJS    = c_funcs.o
LIBOBJS  = $(sort fw_sysgtk.o fw_common.o fw_syscti.o fw_menusys.o\
	   fw_color.o fw_window.o fw_menu.o fw_menuitem.o \
	   fw_msg.o fw_font.o fw_msgbar.o fw_bar.o fw_btnbmp.o \
	   fw_control.o fw_dialog.o fw_combobox.o fw_get.o fw_say.o fw_button.o\
	   fw_meter.o fw_listbox.o fw_msgitem.o fw_timer.o fw_rect.o\
	   fw_mdiclien.o fw_mdichild.o fw_mdiframe.o fw_scrllbar.o fw_tabs.o\
	   fw_wbrowse.o )

OBJS = $(sort $(COBJS) $(LIBOBJS) )
.SUFFIXES: .prg .o .po


all:  Makefile links  $(STARGET)

cfg :
	mkdir -p $(Clip_I_Dir)/$(PROG)
	cp --remove-destination -fpu$(V) ./include/*.ch $(Clip_I_Dir)/$(PROG)/
	cp --remove-destination -fpu$(V) ./include/*.h $(Clip_I_Dir)/$(PROG)/

links :

#$(FWINLIB): $(COBJS) $(LIBOBJS)
#	$(CC) $(C_FLAGS) -c c_funcs.c
#	$(CLIP_MAKELIB) $(FWLIB) $(LIBOBJS) $(COBJS)
#	$(CLIP_MAKESLIB) $(FWINLIB) $(FWLIB)

cleanc:
	rm -f$(V) fw*.c $(TARGET).* Makefile.inc

distcleanc :
	rm -rf$(V) *.g

install: all install-lib install-example
#	$(CLIP_CP) $(FWINLIB) $(DESTDIR)/lib
#	$(CLIP_CP) libclip-fw $(DESTDIR)/lib
#	mkdir -p $(DESTDIR)/include/fwin
#	mkdir -p$(V) $(DESTDIR)/doc/example/clip-fw
#	mkdir -p$(V) $(DESTDIR)/doc/example/clip-fw/bitmaps
#	mkdir -p$(V) $(DESTDIR)/doc/example/clip-fw/data
#	cp -Ruf$(V) example/* example/.c* $(DESTDIR)/doc/example/clip-fw/

include $(Makefile_o_in)

include $(Makefile_end_in)

