#
#                                            clip-dbtcp/Makefile.in
#
#  GNU make makefile
#
Local_Depend =

PROG = clip-dbtcp
DBTCP_PATH=@DBTCP_PATH@
ADDLIBS=$(DBTCP_PATH)/libs/libdbftp.a
DBTCPINCLUDE=-I$(DBTCP_PATH) -I$(DBTCP_PATH)/utils

C_FLAGS += $(DBTCPINCLUDE)
#TARGET=libclip-dbtcp$(DLLSUFF)
#RTARGET=libclip-dbtcp$(DLLREALSUFF)
OBJS= $(sort dbtcp_clip.o dbtcp.o )

all: Makefile  $(STARGET)

cfg :

#$(TARGET): $(OBJS)
#	$(CLIP_MAKESLIB) $(TARGET) $(OBJS) $(LIBDBTCP)

include $(Makefile_o_in)

install:all install-lib install-example
#	$(CLIP_CP) $(TARGET) $(DESTDIR)/lib

cleanc:
	rm -rf$(V) dbtcp.c dbtcp_ref.c $(TARGET).*

distclean: clean
	rm -f$(V) Makefile

include $(Makefile_end_in)

