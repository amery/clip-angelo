#
#                                            clip-rtf/Makefile.in
#
#  GNU make makefile
#

Local_Depend = include/*.ch
#CLIP	= $(CLIPROOT)/bin/clip
CLIPFLAGS += -n
CLIPLIBS  =
OBJS  = richtext.o
PROG = clip-rtf
#RTLIB = libclip-rtf.a
#RTFLIB = libclip-rtf$(DLLSUFF)
#RTFLIBREAL = libclip-rtf$(DLLREALSUFF)
ADDLIBS =

DOC = readme*
.SUFFIXES: .prg .o .po

all : Makefile $(STARGET)

cfg :
	$(E_cp)cp --remove-destination -fpu$(V) include/richtext.ch $(Clip_I_Dir)/
	$(E_cp)cp --remove-destination -fpu$(V) include/objects.ch $(Clip_I_Dir)/

cleanc :
	$(E_rm)rm -f$(V) *.c $(TARGET).*

distcleanc :
	$(E_rm)rm -rf$(V) *.g

install : all install-lib install-example install-doc

include $(Makefile_o_in)

include $(Makefile_end_in)

