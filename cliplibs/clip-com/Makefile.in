#
#                                            clip-com/Makefile.in
#

Local_Depend = libezV24/*.h
PROG = clip-com
#CLIPINCLUDE=-I$(CLIPROOT)/include

#C_FLAGS=-Wall -g -I. $(CLIPINCLUDE)
#CC=gcc
#TARGET=libclip-com$(DLLSUFF)
#ATARGET=libclip-com.a
#RTARGET=libclip-com$(DLLREALSUFF)
OBJS=com.o
C_FLAGS+=-IlibezV24
LIBEZ=libezV24/libezV24-0.a
ADDLIBS=libezV24/libezV24-0.a

DIRS_TO_SCAN =
# libezV24
INCDIR=
RELEASE=
export INCDIR RELEASE

all: Makefile  have_lib $(STARGET)

cfg :

have_lib:
	true
#	test -f /usr/include/bzlib.h

#$(TARGET): $(OBJS) $(LIBEZ)
#	$(CLIP_MAKESLIB) $(TARGET) $(OBJS) $(LIBEZ)

#$(ATARGET): $(OBJS) $(LIBEZ)
#	$(CLIP_MAKELIB) $(ATARGET) $(OBJS) $(LIBEZ)

$(LIBEZ):
	cd libezV24 && $(MAKE)

include $(Makefile_o_in)

#hashcode.h:
#	echo '#define HASH_ferror ' $$(echo ferror| $(CLIPROOT)/bin/cliphash | cut -d' ' -f2) >hashcode.h

install : all install-lib install-example
#	$(CLIP_CP) $(TARGET) $(DESTDIR)/lib
#	mkdir -p$(V) $(DESTDIR)/doc/clip-com
#	mkdir -p$(V) $(DESTDIR)/doc/example/clip-com
#	cp -Ruf$(V) example/* $(DESTDIR)/doc/example/clip-com/

cleanc : libezV24 example
	rm -rf$(V) hashcode.h $(TARGET).*
	cd libezV24; $(MAKE) clean; rm -f$(V) *.a *.so *.so.* *.exe

distclean: clean

include $(Makefile_end_in)


