#
#                                            clip-gzip/example/Makefile.in
#
Local_Depend =
# $(Clip_L_Dir)/libclip-gzip.so

PRG=gz$(EXESUFF)

all:Makefile  $(PRG)

install : all

cfg :

cleanc:
	$(E_rm)rm -f$(V) *.c *.b $(PRG) *.gz

distcleanc :
	$(E_rm)rm -rf$(V) *.g

gz$(EXESUFF): gz.prg $(CLIP) $(LIBCLIP)
	$(Trace_clip)
	$(CLIP) -o$@ -ew -s -l -M gz.prg -lclip-gzip

include $(Makefile_o_in)

include $(Makefile_end_in)

