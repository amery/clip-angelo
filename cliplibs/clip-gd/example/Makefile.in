#
#                                            clip-gd/example/Makefile.in
#
Depend_on 			=			 $(Clip_L_Dir)/libclip-gd.so
PRG 					= 			gr$(EXESUFF)

export

######################################################################################
###################################################################################### local rules

all: Makefile $(PRG)

install : all

hash1 :

cfg :

cleanc:
	$(E_rm)rm -f$(V) *.c *.b $(PRG)

distcleanc :
	$(E_rm)rm -rf$(V) *.g

gr$(EXESUFF): gr.prg $(CLIP) $(LIBCLIP) $(Depend_on)
	$(E_e)$(Trace_clip)
	$(CLIP) -o$@ -swlN -e -M gr.prg -lclip-gd $(LIBS)
# -lgd

include $(Makefile_o_in)

include $(Makefile_end_in)

