##
##										cliplibs/Makefile.in
##
#
Local_Depend =
include ./Makefile.01
#ifeq ($(MAKECMDGOALS),clean)
#DIRS_TO_SCAN = clip-bzip2 		clip-codb 			clip-com 			clip-crypto  	\
#					clip-cti 		 																	\
#					clip-fcgi 																			\
#					clip-gd 			clip-glade2 													\
#					clip-gtk2 																			\
#					clip-dbtcp 																			\
#					clip-fw 																				\
#					clip-gzip 		clip-mysql 			clip-odbc 								\
#					clip-nanfor 	clip-netto 														\
#					clip-oracle 	clip-postgres 		clip-postscript 	clip-r2d2 		\
#					clip-rtf 		clip-xml 			clip-ui
#endif
export CFLAGS CLIP CLIPFLAGS CLIPROOT
#					clip-gtk 																			\
#					clip-gtkextra 																			\

################################################################################################################

all local user system install lib : Makefile $(DIRS_TO_SCAN) FORCE
	$(E_e)echo $@ $<
	$(E_xx)banner extended 		>&0
	$(E_xx)banner libraries 	>&0
	$(E_xx)banner are OK 		>&0
	$(E_xx)banner extended 		>&1
	$(E_xx)banner libraries 	>&1
	$(E_xx)banner are OK 		>&1
	$(E_xx)banner extended 		>&2
	$(E_xx)banner libraries 	>&2
	$(E_xx)banner are OK 		>&2

./Makefile.01 : ./configure ./configure.in
	$(E_xx)./configure

cfg :

cleanc :

distcleanc:
	$(E_rm)rm -rf$(V) *.g
	$(E_rm)rm -f$(V) *.o *.n

deb: clean
	$(E_xx)for dir in $(DIRS_TO_SCAN); \
		do ./mkdeb $$dir; \
	done

rpm:
	$(E_xx)for dir in $(DIRS_TO_SCAN); \
		do ./mkrpm $$dir; \
	done

rpm-opt:
	$(E_xx)for dir in $(DIRS_TO_SCAN); \
		do ./mkrpm -opt $$dir; \
	done

tgz tbz:
	$(E_xx)for dir in $(DIRS_TO_SCAN); \
		do ./mktgz $$dir; \
	done

include $(Makefile_end_in)
