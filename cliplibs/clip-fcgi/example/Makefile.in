#
#                                            clip-fcgi/example/Makefile.in
#
Local_Depend =
# $(Clip_L_Dir)/libclip-fcgi.so

PRG=clip_echo.fcgi

################################################################################################################

all: Makefile $(PRG)$(EXESUFF)

cfg :

cleanc:
	$(E_rm)rm -f$(V) *.c *.b $(PRG)

distcleanc :
	$(E_rm)rm -rf$(V) *.g

$(PRG)$(EXESUFF): $(PRG).prg $(CLIP) $(LIBCLIP)
	$(E_e)$(Trace_clip)
	$(CLIP) -o$@ -e -M $(PRG).prg -lclip-fcgi

include $(Makefile_o_in)

include $(Makefile_end_in)

