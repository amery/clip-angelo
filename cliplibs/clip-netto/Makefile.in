#
#                                            clip-netto/Makefile.in
#

Local_Depend = include/*.ch
PROG = clip-netto
#TARGET=libclip-netto$(DLLSUFF)
#RTARGET=libclip-netto$(DLLREALSUFF)
#LIBS=libclip-netto.a
OBJS  = $(sort fnbin2i.o fndpve.o fnerr.o fnhex.o \
	namel.o nonull.o \
	nwdate.o reg2l.o ssfp.o \
	)
# fnbit.o fnpfeval.o is3x.o isnet.o ipxinit.o request.o fnpeek.o
DOC = doc/*
ADDLIBS =

all : Makefile $(STARGET)

cfg :
	$(E_mkD)mkdir -p$(V) $(Clip_I_Dir)/$(PROG)
	$(E_cp)cp --remove-destination -fpu$(V) include/*.ch include/*.h $(Clip_I_Dir)/$(PROG)

#$(TARGET):$(DIRS_TO_SCAN)
#	$(CLIP_MAKESLIB) $(TARGET) $(LIBS)

distcleanc :
	$(E_rm)rm -rf$(V) *.g

cleanc :
	$(E_rm)rm -f$(V) $(TARGET).* *.c

install : all install-lib install-doc
#	$(CLIP_CP) $(TARGET) $(DESTDIR)/lib
#	$(CLIP_CP) $(LIBS) $(DESTDIR)/lib
#	mkdir -p$(V) $(DESTDIR)/include/netto

include $(Makefile_o_in)

include $(Makefile_end_in)

